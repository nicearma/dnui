'use strict';

angular
    .module('dnuiPlugin', [
        'ngResource',
        'ngAnimate',
        'ui.bootstrap.pagination',
        'ui.bootstrap.tabs',
        'ui.bootstrap.modal'
    ]);
    /*.factory('$exceptionHandler', ['$rootScope', function ($rootScope) {

        return function (exception, cause) {
            $rootScope.$broadcast('error', {exception: exception, cause: cause});

        };
    }]
);*/

